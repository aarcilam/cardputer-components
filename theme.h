#pragma once

namespace Theme {
  const uint32_t PRIMARY_COLOR = 0x8A2BE2;      // Violeta azulado
  const uint32_t SECONDARY_COLOR = 0x9370DB;    // Violeta medio
  const uint32_t BACKGROUND_COLOR = 0x4B0082;   // Índigo
  const uint32_t TEXT_COLOR = 0xE6E6FA;         // Lavanda claro
  const uint32_t ACCENT_COLOR = 0xBA55D3;       // Orquídea medio

  const int FONT_TITLE = 2;
  const int FONT_NORMAL = 2; 
  const int PADDING = 6;
}
